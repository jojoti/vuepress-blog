version: '3'

services:

  docs:
    build: .
    restart: always
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./docs:/home/docs/docs:rw
    command:
      - /bin/sh
      - -c
      - |
        yarn docs:dev